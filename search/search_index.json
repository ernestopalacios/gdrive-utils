{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"gdrive-utils","text":"<p>A reusable Python library for Google Drive/Sheets operations and file-renaming workflows.</p>"},{"location":"#what-is-it","title":"What is it?","text":"<p><code>gdrive-utils</code> was extracted from the original <code>organizar.py</code> script so it can be shared across multiple projects via <code>uv add</code> or <code>pip install</code>.</p> <p>The library provides two APIs:</p> <ul> <li>Modern API (recommended for new projects) \u2014 typed functions that raise custom exceptions and accept a <code>GDriveConfig</code> object.</li> <li>Compatible API (drop-in replacement) \u2014 the original function names and string-based error returns so existing code keeps working.</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>uv add git+https://github.com/ernestopalacios/gdrive-utils.git\n</code></pre> <p>Or with pip:</p> <pre><code>pip install git+https://github.com/ernestopalacios/gdrive-utils.git\n</code></pre>"},{"location":"#quick-example","title":"Quick example","text":"<pre><code>from gdrive_utils import GDriveConfig, read_worksheet, build_filename\n\nconfig = GDriveConfig(\n    credentials_path=\"secrets/gdrive_credentials.json\",\n    spreadsheet_name=\"DB_calificar_ot\",\n    worksheet_name=\"Iniciales\",\n)\n\ndf = read_worksheet(config)\n\ndata = {\n    \"cuadrilla\": \"Z1 Cuadrilla A\",\n    \"responsable\": [\"Juan Perez Garcia\"],\n    \"fecha\": \"2024-01-15T10:30:00\",\n}\nfilename = build_filename(data, df)\nprint(filename)  # OT [10] Cuadrilla 1 2024-01-15 (1) GJ.pdf\n</code></pre> <p>See the User Guide for more details.</p>"},{"location":"api/","title":"API Reference","text":"<p>The library is organised into several modules:</p> Module Purpose <code>gdrive_utils.exceptions</code> Custom exception hierarchy <code>gdrive_utils.config</code> Configuration dataclass <code>gdrive_utils.auth</code> Google authentication <code>gdrive_utils.sheets</code> Google Sheets reading <code>gdrive_utils.core</code> Data-processing functions <code>gdrive_utils.files</code> File operations <code>gdrive_utils.compat</code> Backward-compatible wrappers"},{"location":"api/#modern-api-overview","title":"Modern API overview","text":"<pre><code>from gdrive_utils import (\n    GDriveConfig,\n    read_worksheet,\n    get_responsable_order,\n    get_cuadrilla_order,\n    get_initials,\n    get_cuadrilla_by_name,\n    get_short_cuadrilla_name,\n    build_filename,\n    move_file,\n)\n</code></pre>"},{"location":"api/#compatible-api-overview","title":"Compatible API overview","text":"<pre><code>from gdrive_utils import (\n    get_gsheet_df,\n    get_num_responsable,\n    get_num_cuadrilla,\n    get_iniciales,\n    from_name_get_cuadrilla,\n    get_nombre_corto_cuadrilla,\n    get_nombre_archivo,\n    renombrar_ot,\n)\n</code></pre> <p>See the individual module pages for full API details.</p>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#020-2026-06-28","title":"0.2.0 (2026-06-28)","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>get_all_data(value, column, df)</code> \u2014 look up a full row by any column and   receive every field as a dictionary. <code>USER_ID</code> is returned as <code>int</code>; all   other fields are strings. Raises <code>DataFrameError</code> if the column is missing   or no match is found.</li> </ul>"},{"location":"changelog/#010-2026-06-28","title":"0.1.0 (2026-06-28)","text":""},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li>Initial release extracted from <code>organizar.py</code>.</li> <li>Modern API with typed exceptions and <code>GDriveConfig</code>.</li> <li>Compatible API preserving original function names and string error returns.</li> <li>Google-auth authentication (replaced deprecated <code>oauth2client</code>).</li> <li>Test suite with <code>pytest</code> and coverage reporting.</li> <li>CI pipeline via GitHub Actions.</li> <li>Documentation site powered by MkDocs + Material + mkdocstrings.</li> </ul>"},{"location":"configuration/","title":"Configuration","text":""},{"location":"configuration/#gdriveconfig","title":"<code>GDriveConfig</code>","text":"<p>All Google Drive / Sheets operations accept an optional <code>GDriveConfig</code> object. When <code>None</code> is passed, a default instance is created.</p> <pre><code>from gdrive_utils import GDriveConfig\n\nconfig = GDriveConfig()\n</code></pre>"},{"location":"configuration/#attributes","title":"Attributes","text":"Attribute Default Description <code>credentials_path</code> <code>secrets/gdrive_credentials.json</code> Path to the service-account JSON file. Can be overridden via the <code>GDRIVE_CREDENTIALS_PATH</code> environment variable. <code>spreadsheet_name</code> <code>\"DB_calificar_ot\"</code> Name of the Google Sheet to open. <code>worksheet_name</code> <code>\"Iniciales\"</code> Name of the worksheet tab inside the sheet. <code>scope</code> <code>[\"spreadsheets\", \"drive\"]</code> OAuth scopes requested."},{"location":"configuration/#environment-variable-override","title":"Environment variable override","text":"<pre><code>export GDRIVE_CREDENTIALS_PATH=/path/to/credentials.json\n</code></pre> <pre><code>from gdrive_utils import GDriveConfig\n\nconfig = GDriveConfig()\nassert config.credentials_path == \"/path/to/credentials.json\"\n</code></pre> <p>You can still pass an explicit path if needed:</p> <pre><code>config = GDriveConfig(credentials_path=\"other.json\")\n</code></pre>"},{"location":"configuration/#credentials-file","title":"Credentials file","text":"<p>Download a service-account key from Google Cloud Console and place it at the path specified by <code>credentials_path</code>.</p> <p>Remember to share the target Google Sheet with the service-account email address (visible in the JSON file under <code>client_email</code>).</p>"},{"location":"contributing/","title":"Contributing","text":""},{"location":"contributing/#development-setup","title":"Development setup","text":"<pre><code>git clone https://github.com/ernestopalacios/gdrive-utils.git\ncd gdrive-utils\nuv sync --group dev\n</code></pre>"},{"location":"contributing/#running-tests","title":"Running tests","text":"<pre><code>uv run pytest -v\n</code></pre>"},{"location":"contributing/#linting-and-type-checking","title":"Linting and type checking","text":"<pre><code>uv run ruff check src tests\nuv run mypy src\n</code></pre>"},{"location":"contributing/#building-documentation","title":"Building documentation","text":"<pre><code>uv run mkdocs build\n</code></pre> <p>Preview locally:</p> <pre><code>uv run mkdocs serve\n</code></pre>"},{"location":"contributing/#ci","title":"CI","text":"<p>Pull requests trigger GitHub Actions that run tests, linting, and type checking on Python 3.10, 3.11, and 3.12.</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10 or higher</li> <li>A Google service-account JSON credentials file</li> <li>The target Google Sheet must be shared with the service-account email</li> </ul>"},{"location":"installation/#install-from-git","title":"Install from Git","text":"<pre><code>uv add git+https://github.com/ernestopalacios/gdrive-utils.git\n</code></pre> <p>With pip:</p> <pre><code>pip install git+https://github.com/ernestopalacios/gdrive-utils.git\n</code></pre>"},{"location":"installation/#install-with-development-dependencies","title":"Install with development dependencies","text":"<p>If you want to contribute or run tests locally:</p> <pre><code>git clone https://github.com/ernestopalacios/gdrive-utils.git\ncd gdrive-utils\nuv sync --group dev\n</code></pre> <p>This installs:</p> <ul> <li><code>pytest</code> + <code>pytest-cov</code> \u2014 testing</li> <li><code>ruff</code> \u2014 linting</li> <li><code>mypy</code> \u2014 type checking</li> <li><code>mkdocs-material</code> + <code>mkdocstrings[python]</code> \u2014 documentation</li> </ul>"},{"location":"quickstart/","title":"Quick Start","text":""},{"location":"quickstart/#modern-api-recommended","title":"Modern API (recommended)","text":"<p>Import the typed functions and create a <code>GDriveConfig</code>:</p> <pre><code>from gdrive_utils import GDriveConfig, read_worksheet, build_filename, move_file\n\n# Configuration is read from env var GDRIVE_CREDENTIALS_PATH or uses defaults.\nconfig = GDriveConfig(\n    spreadsheet_name=\"DB_calificar_ot\",\n    worksheet_name=\"Iniciales\",\n)\n\ndf = read_worksheet(config)\n\ndata = {\n    \"cuadrilla\": \"Z1 Cuadrilla A\",\n    \"responsable\": [\"Juan Perez Garcia\"],\n    \"fecha\": \"2024-01-15T10:30:00\",\n}\nfilename = build_filename(data, df)\nnew_path = move_file(\"/path/to/old.pdf\", filename)\n</code></pre>"},{"location":"quickstart/#error-handling","title":"Error handling","text":"<p>The modern API raises typed exceptions:</p> Exception When it happens <code>AuthenticationError</code> Credentials file missing or invalid <code>SpreadsheetNotFoundError</code> Google Sheet not found or not shared <code>WorksheetNotFoundError</code> Worksheet tab does not exist <code>DataFrameError</code> Required columns missing or sheet empty <code>FileOperationError</code> File move/rename fails <pre><code>from gdrive_utils import (\n    read_worksheet,\n    AuthenticationError,\n    SpreadsheetNotFoundError,\n)\n\ntry:\n    df = read_worksheet()\nexcept AuthenticationError:\n    print(\"Check your credentials file\")\nexcept SpreadsheetNotFoundError:\n    print(\"Make sure the sheet is shared with the service account\")\n</code></pre>"},{"location":"quickstart/#compatible-api-drop-in","title":"Compatible API (drop-in)","text":"<p>If you already have code using the original <code>organizar.py</code> functions, simply change the import:</p> <pre><code>from gdrive_utils import (\n    get_gsheet_df,\n    get_num_responsable,\n    get_num_cuadrilla,\n    get_iniciales,\n    from_name_get_cuadrilla,\n    get_nombre_corto_cuadrilla,\n    get_nombre_archivo,\n    renombrar_ot,\n)\n</code></pre> <p>All function names, signatures, and string error returns remain identical.</p>"},{"location":"quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Read about Configuration</li> <li>Browse the API Reference</li> </ul>"},{"location":"api/auth/","title":"Authentication","text":""},{"location":"api/auth/#gdrive_utils.auth","title":"gdrive_utils.auth","text":"<p>Google authentication using modern google-auth.</p>"},{"location":"api/auth/#gdrive_utils.auth-classes","title":"Classes","text":""},{"location":"api/auth/#gdrive_utils.auth-functions","title":"Functions:","text":""},{"location":"api/auth/#gdrive_utils.auth.authorize","title":"authorize","text":"<pre><code>authorize(config=None)\n</code></pre> <p>Authenticate with Google and return an authorised gspread client.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>GDriveConfig | None</code> <p>A :class:<code>GDriveConfig</code> instance. If <code>None</code>, default configuration is used.</p> <code>None</code> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the credentials file is missing or invalid.</p> Source code in <code>src/gdrive_utils/auth.py</code> <pre><code>def authorize(config: GDriveConfig | None = None) -&gt; gspread.Client:\n    \"\"\"Authenticate with Google and return an authorised *gspread* client.\n\n    Args:\n        config: A :class:`GDriveConfig` instance. If ``None``, default\n            configuration is used.\n\n    Raises:\n        AuthenticationError: If the credentials file is missing or invalid.\n\n    \"\"\"\n    if config is None:\n        config = GDriveConfig()\n\n    creds_path = Path(config.credentials_path)\n    if not creds_path.exists():\n        raise AuthenticationError(\n            f\"Credentials file not found: {creds_path.resolve()}\"\n        )\n\n    try:\n        credentials = Credentials.from_service_account_file(\n            str(creds_path), scopes=config.scope\n        )\n        client = gspread.authorize(credentials)\n        logger.info(\"Authentication successful!\")\n        return client\n    except (GoogleAuthError, ValueError, OSError) as exc:\n        logger.error(\"Authentication failed: %s\", exc)\n        raise AuthenticationError(f\"Authentication failed: {exc}\") from exc\n</code></pre>"},{"location":"api/compat/","title":"Compatibility","text":""},{"location":"api/compat/#gdrive_utils.compat","title":"gdrive_utils.compat","text":"<p>Backward-compatible wrappers from the original organizar.py.</p> <p>These keep the exact function names and string-based error returns. New projects should use the modern API in :mod:<code>gdrive_utils.sheets</code>, :mod:<code>gdrive_utils.core</code>, and :mod:<code>gdrive_utils.files</code> instead.</p>"},{"location":"api/compat/#gdrive_utils.compat-classes","title":"Classes","text":""},{"location":"api/compat/#gdrive_utils.compat-functions","title":"Functions:","text":""},{"location":"api/compat/#gdrive_utils.compat.get_gsheet_df","title":"get_gsheet_df","text":"<pre><code>get_gsheet_df(df=None)\n</code></pre> <p>Original wrapper: returns a DataFrame or the string <code>'Fail'</code>.</p> <p>Parameters are ignored for backward compatibility; the default :class:<code>GDriveConfig</code> is used.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def get_gsheet_df(df: pd.DataFrame | None = None) -&gt; pd.DataFrame | str:\n    \"\"\"Original wrapper: returns a DataFrame or the string ``'Fail'``.\n\n    Parameters are ignored for backward compatibility; the default\n    :class:`GDriveConfig` is used.\n    \"\"\"\n    try:\n        return read_worksheet(GDriveConfig())\n    except AuthenticationError:\n        logger.info(\"Authentication failed\")\n        return \"Fail\"\n    except SpreadsheetNotFoundError:\n        logger.info(\n            \"Error: Spreadsheet not found. \"\n            \"Make sure the name is correct and the sheet is shared with the service account email.\"\n        )\n        return \"Fail\"\n    except WorksheetNotFoundError:\n        logger.info(\"Error: Worksheet not found in the spreadsheet.\")\n        return \"Fail\"\n    except DataFrameError:\n        logger.info(\"Error: The selected worksheet appears to be empty.\")\n        return \"Fail\"\n    except Exception as exc:\n        logger.info(\"An error occurred while accessing the sheet/worksheet: %s\", exc)\n        return \"Fail\"\n</code></pre>"},{"location":"api/compat/#gdrive_utils.compat.get_num_responsable","title":"get_num_responsable","text":"<pre><code>get_num_responsable(nombre, df)\n</code></pre> <p>Original wrapper: returns order string, <code>'0'</code>, or <code>'x'</code>.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def get_num_responsable(nombre: str, df: pd.DataFrame | str) -&gt; str:\n    \"\"\"Original wrapper: returns order string, ``'0'``, or ``'x'``.\"\"\"\n    if not isinstance(df, pd.DataFrame):\n        return \"x\"\n    try:\n        return get_responsable_order(nombre, df)\n    except DataFrameError as exc:\n        logger.info(\"DataFrame error in get_num_responsable: %s\", exc)\n        return \"x\"\n</code></pre>"},{"location":"api/compat/#gdrive_utils.compat.get_num_cuadrilla","title":"get_num_cuadrilla","text":"<pre><code>get_num_cuadrilla(cuadrilla_ot, df)\n</code></pre> <p>Original wrapper: returns order string, <code>'no'</code>, or <code>'XX'</code>.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def get_num_cuadrilla(cuadrilla_ot: str, df: pd.DataFrame | str) -&gt; str:\n    \"\"\"Original wrapper: returns order string, ``'no'``, or ``'XX'``.\"\"\"\n    if not isinstance(df, pd.DataFrame):\n        return \"XX\"\n    try:\n        return get_cuadrilla_order(cuadrilla_ot, df)\n    except DataFrameError as exc:\n        logger.info(\"DataFrame error in get_num_cuadrilla: %s\", exc)\n        return \"no\"\n</code></pre>"},{"location":"api/compat/#gdrive_utils.compat.get_iniciales","title":"get_iniciales","text":"<pre><code>get_iniciales(nombre, df='vacio')\n</code></pre> <p>Original wrapper: returns initials or <code>'revisar_nombre'</code>.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def get_iniciales(nombre: str, df: pd.DataFrame | str = \"vacio\") -&gt; str:\n    \"\"\"Original wrapper: returns initials or ``'revisar_nombre'``.\"\"\"\n    if isinstance(df, str) and df == \"vacio\":\n        df = pd.DataFrame()  # empty DataFrame triggers calculation path\n    if not isinstance(df, pd.DataFrame):\n        return \"revisar_nombre\"\n    try:\n        return get_initials(nombre, df)\n    except DataFrameError:\n        return \"revisar_nombre\"\n</code></pre>"},{"location":"api/compat/#gdrive_utils.compat.from_name_get_cuadrilla","title":"from_name_get_cuadrilla","text":"<pre><code>from_name_get_cuadrilla(nombre, df)\n</code></pre> <p>Original wrapper: returns cuadrilla name or <code>'no'</code>.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def from_name_get_cuadrilla(nombre: str, df: pd.DataFrame | str) -&gt; str:\n    \"\"\"Original wrapper: returns cuadrilla name or ``'no'``.\"\"\"\n    if not isinstance(df, pd.DataFrame):\n        return \"no\"\n    try:\n        return get_cuadrilla_by_name(nombre, df)\n    except DataFrameError as exc:\n        logger.info(\"DataFrame error in from_name_get_cuadrilla: %s\", exc)\n        return \"no\"\n</code></pre>"},{"location":"api/compat/#gdrive_utils.compat.get_nombre_corto_cuadrilla","title":"get_nombre_corto_cuadrilla","text":"<pre><code>get_nombre_corto_cuadrilla(cuadrilla, df='vacio')\n</code></pre> <p>Original wrapper: returns short name or the original string.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def get_nombre_corto_cuadrilla(cuadrilla: str, df: pd.DataFrame | str = \"vacio\") -&gt; str:\n    \"\"\"Original wrapper: returns short name or the original string.\"\"\"\n    if isinstance(df, str) and df == \"vacio\":\n        df = pd.DataFrame()\n    if not isinstance(df, pd.DataFrame):\n        return cuadrilla\n    try:\n        return get_short_cuadrilla_name(cuadrilla, df)\n    except DataFrameError:\n        return cuadrilla\n</code></pre>"},{"location":"api/compat/#gdrive_utils.compat.get_nombre_archivo","title":"get_nombre_archivo","text":"<pre><code>get_nombre_archivo(obj, df='vacio')\n</code></pre> <p>Original wrapper that accepts the old obj interface.</p> <p>On any error calls <code>obj.Log2Ot</code> and returns <code>os.path.basename(obj.link)</code>.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def get_nombre_archivo(obj: Any, df: pd.DataFrame | str = \"vacio\") -&gt; str:\n    \"\"\"Original wrapper that accepts the old *obj* interface.\n\n    On any error calls ``obj.Log2Ot`` and returns ``os.path.basename(obj.link)``.\n    \"\"\"\n    if isinstance(df, str) and df == \"vacio\":\n        df = pd.DataFrame()\n    if not isinstance(df, pd.DataFrame):\n        df = pd.DataFrame()\n\n    try:\n        if not getattr(obj, \"valido\", True):\n            return str(os.path.basename(obj.link))\n\n        data = {\n            \"cuadrilla\": obj.data[\"cuadrilla\"],\n            \"responsable\": obj.data[\"responsable\"],\n            \"fecha\": obj.data[\"fecha\"],\n        }\n        return build_filename(data, df)\n    except Exception as exc:\n        logger.info(\"No fue posible renombrar la OT Error: %s\", exc)\n        obj.Log2Ot(\n            \"ERROR\",\n            \"No fue posible renombrar la OT\",\n            \"No se pudo extraer la informaci\u00f3n de la Orden de Trabajo para ser renombrada\",\n        )\n        return str(os.path.basename(obj.link))\n</code></pre>"},{"location":"api/compat/#gdrive_utils.compat.renombrar_ot","title":"renombrar_ot","text":"<pre><code>renombrar_ot(current_file_path, nombre_nuevo)\n</code></pre> <p>Original wrapper: returns destination path or <code>'Failed'</code>.</p> Source code in <code>src/gdrive_utils/compat.py</code> <pre><code>def renombrar_ot(current_file_path: str, nombre_nuevo: str) -&gt; str:\n    \"\"\"Original wrapper: returns destination path or ``'Failed'``.\"\"\"\n    try:\n        return _move_file(current_file_path, nombre_nuevo)\n    except FileOperationError as exc:\n        logger.info(\"Error moving/renaming file: %s\", exc)\n        return \"Failed\"\n    except Exception as exc:\n        logger.info(\"Error: %s\", exc)\n        return \"Failed\"\n</code></pre>"},{"location":"api/config/","title":"Configuration","text":""},{"location":"api/config/#gdrive_utils.config","title":"gdrive_utils.config","text":"<p>Configuration dataclass for gdrive-utils.</p>"},{"location":"api/config/#gdrive_utils.config-classes","title":"Classes","text":""},{"location":"api/config/#gdrive_utils.config.GDriveConfig","title":"GDriveConfig  <code>dataclass</code>","text":"<pre><code>GDriveConfig(\n    credentials_path=(\n        lambda: os.getenv(\n            \"GDRIVE_CREDENTIALS_PATH\",\n            \"secrets/gdrive_credentials.json\",\n        )\n    )(),\n    spreadsheet_name=\"DB_calificar_ot\",\n    worksheet_name=\"Iniciales\",\n    scope=(\n        lambda: [\n            \"https://www.googleapis.com/auth/spreadsheets\",\n            \"https://www.googleapis.com/auth/drive\",\n        ]\n    )(),\n)\n</code></pre> <p>Configuration for Google Drive/Sheets operations.</p> <p>The credentials_path defaults to the value of the environment variable <code>GDRIVE_CREDENTIALS_PATH</code> (if set), otherwise falls back to <code>secrets/gdrive_credentials.json</code>.</p>"},{"location":"api/core/","title":"Core","text":""},{"location":"api/core/#gdrive_utils.core","title":"gdrive_utils.core","text":"<p>Core data-processing functions (modern API).</p> <p>These functions accept plain DataFrames and raise custom exceptions on errors.</p>"},{"location":"api/core/#gdrive_utils.core-classes","title":"Classes","text":""},{"location":"api/core/#gdrive_utils.core-functions","title":"Functions:","text":""},{"location":"api/core/#gdrive_utils.core.get_responsable_order","title":"get_responsable_order","text":"<pre><code>get_responsable_order(name, df)\n</code></pre> <p>Return the ORDEN_RESPONSABLE value for name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The person's full name.</p> required <code>df</code> <code>DataFrame</code> <p>The personnel DataFrame.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The order string, or <code>'0'</code> if the name is not found.</p> <p>Raises:</p> Type Description <code>DataFrameError</code> <p>If df is not a DataFrame or required columns are missing.</p> Source code in <code>src/gdrive_utils/core.py</code> <pre><code>def get_responsable_order(name: str, df: pd.DataFrame) -&gt; str:\n    \"\"\"Return the *ORDEN_RESPONSABLE* value for *name*.\n\n    Args:\n        name: The person's full name.\n        df: The personnel DataFrame.\n\n    Returns:\n        The order string, or ``'0'`` if the name is not found.\n\n    Raises:\n        DataFrameError: If *df* is not a DataFrame or required columns are missing.\n\n    \"\"\"\n    if not isinstance(df, pd.DataFrame):\n        raise DataFrameError(\"Expected a pandas DataFrame\")\n    if \"NOMBRE\" not in df.columns or \"ORDEN_RESPONSABLE\" not in df.columns:\n        raise DataFrameError(\"Required columns missing: NOMBRE, ORDEN_RESPONSABLE\")\n\n    series = df.query(f\"NOMBRE == {name!r}\")[\"ORDEN_RESPONSABLE\"]\n    if not series.empty:\n        return str(series.iloc[0])\n    return \"0\"\n</code></pre>"},{"location":"api/core/#gdrive_utils.core.get_cuadrilla_order","title":"get_cuadrilla_order","text":"<pre><code>get_cuadrilla_order(cuadrilla_ot, df)\n</code></pre> <p>Return the ORDEN_CUADRILLA value for cuadrilla_ot.</p> <p>Parameters:</p> Name Type Description Default <code>cuadrilla_ot</code> <code>str</code> <p>The cuadrilla identifier.</p> required <code>df</code> <code>DataFrame</code> <p>The personnel DataFrame.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The order string, or <code>'no'</code> if not found.</p> <p>Raises:</p> Type Description <code>DataFrameError</code> <p>If df is not a DataFrame or required columns are missing.</p> Source code in <code>src/gdrive_utils/core.py</code> <pre><code>def get_cuadrilla_order(cuadrilla_ot: str, df: pd.DataFrame) -&gt; str:\n    \"\"\"Return the *ORDEN_CUADRILLA* value for *cuadrilla_ot*.\n\n    Args:\n        cuadrilla_ot: The cuadrilla identifier.\n        df: The personnel DataFrame.\n\n    Returns:\n        The order string, or ``'no'`` if not found.\n\n    Raises:\n        DataFrameError: If *df* is not a DataFrame or required columns are missing.\n\n    \"\"\"\n    if not isinstance(df, pd.DataFrame):\n        raise DataFrameError(\"Expected a pandas DataFrame\")\n    if \"CUADRILLA_OT\" not in df.columns or \"ORDEN_CUADRILLA\" not in df.columns:\n        raise DataFrameError(\"Required columns missing: CUADRILLA_OT, ORDEN_CUADRILLA\")\n\n    series = df.query(f\"CUADRILLA_OT == {cuadrilla_ot!r}\")[\"ORDEN_CUADRILLA\"]\n    if not series.empty:\n        return str(series.iloc[0])\n    return \"no\"\n</code></pre>"},{"location":"api/core/#gdrive_utils.core.get_initials","title":"get_initials","text":"<pre><code>get_initials(name, df)\n</code></pre> <p>Return initials for name.</p> <p>If df contains an INICIALES column and the name is found there, the stored value is returned. Otherwise initials are calculated from the name itself, with a collision check against the DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The person's full name (at least three parts recommended).</p> required <code>df</code> <code>DataFrame</code> <p>The personnel DataFrame.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The initials string.</p> <p>Raises:</p> Type Description <code>DataFrameError</code> <p>If df is not a DataFrame.</p> Source code in <code>src/gdrive_utils/core.py</code> <pre><code>def get_initials(name: str, df: pd.DataFrame) -&gt; str:\n    \"\"\"Return initials for *name*.\n\n    If *df* contains an *INICIALES* column and the name is found there, the\n    stored value is returned. Otherwise initials are calculated from the name\n    itself, with a collision check against the DataFrame.\n\n    Args:\n        name: The person's full name (at least three parts recommended).\n        df: The personnel DataFrame.\n\n    Returns:\n        The initials string.\n\n    Raises:\n        DataFrameError: If *df* is not a DataFrame.\n\n    \"\"\"\n    if not isinstance(df, pd.DataFrame):\n        raise DataFrameError(\"Expected a pandas DataFrame\")\n\n    df_has_initials = (\n        \"NOMBRE\" in df.columns and \"INICIALES\" in df.columns\n    )\n\n    if df_has_initials:\n        series = df.query(f\"NOMBRE == {name!r}\")[\"INICIALES\"]\n        if not series.empty:\n            return str(series.iloc[0])\n\n    name_parts = name.split(\" \")\n    if len(name_parts) &lt; 3:\n        return \"revisar_nombre\"\n\n    calculated = name_parts[2][0] + name_parts[0][0]\n\n    if not df_has_initials:\n        return calculated\n\n    # Collision check\n    check = df.query(f\"INICIALES == {calculated!r}\")[\"NOMBRE\"]\n    if check.empty:\n        return calculated\n\n    # Append second name initial to disambiguate\n    return calculated + name_parts[1][0]\n</code></pre>"},{"location":"api/core/#gdrive_utils.core.get_cuadrilla_by_name","title":"get_cuadrilla_by_name","text":"<pre><code>get_cuadrilla_by_name(name, df)\n</code></pre> <p>Return the CUADRILLA_OT value associated with name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The person's full name.</p> required <code>df</code> <code>DataFrame</code> <p>The personnel DataFrame.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The cuadrilla string, or <code>'no'</code> if not found.</p> <p>Raises:</p> Type Description <code>DataFrameError</code> <p>If df is not a DataFrame or required columns are missing.</p> Source code in <code>src/gdrive_utils/core.py</code> <pre><code>def get_cuadrilla_by_name(name: str, df: pd.DataFrame) -&gt; str:\n    \"\"\"Return the *CUADRILLA_OT* value associated with *name*.\n\n    Args:\n        name: The person's full name.\n        df: The personnel DataFrame.\n\n    Returns:\n        The cuadrilla string, or ``'no'`` if not found.\n\n    Raises:\n        DataFrameError: If *df* is not a DataFrame or required columns are missing.\n\n    \"\"\"\n    if not isinstance(df, pd.DataFrame):\n        raise DataFrameError(\"Expected a pandas DataFrame\")\n    if \"NOMBRE\" not in df.columns or \"CUADRILLA_OT\" not in df.columns:\n        raise DataFrameError(\"Required columns missing: NOMBRE, CUADRILLA_OT\")\n\n    series = df.query(f\"NOMBRE == {name!r}\")[\"CUADRILLA_OT\"]\n    if not series.empty:\n        return str(series.iloc[0])\n    return \"no\"\n</code></pre>"},{"location":"api/core/#gdrive_utils.core.get_short_cuadrilla_name","title":"get_short_cuadrilla_name","text":"<pre><code>get_short_cuadrilla_name(cuadrilla, df)\n</code></pre> <p>Return a short display name for cuadrilla.</p> <p>If df contains a CUADRILLA_CORTO column and the cuadrilla is found there, the stored short name is returned. Otherwise a short name is calculated from the cuadrilla string.</p> <p>Parameters:</p> Name Type Description Default <code>cuadrilla</code> <code>str</code> <p>The full cuadrilla name.</p> required <code>df</code> <code>DataFrame</code> <p>The personnel DataFrame.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The short cuadrilla name.</p> <p>Raises:</p> Type Description <code>DataFrameError</code> <p>If df is not a DataFrame.</p> Source code in <code>src/gdrive_utils/core.py</code> <pre><code>def get_short_cuadrilla_name(cuadrilla: str, df: pd.DataFrame) -&gt; str:\n    \"\"\"Return a short display name for *cuadrilla*.\n\n    If *df* contains a *CUADRILLA_CORTO* column and the cuadrilla is found\n    there, the stored short name is returned. Otherwise a short name is\n    calculated from the cuadrilla string.\n\n    Args:\n        cuadrilla: The full cuadrilla name.\n        df: The personnel DataFrame.\n\n    Returns:\n        The short cuadrilla name.\n\n    Raises:\n        DataFrameError: If *df* is not a DataFrame.\n\n    \"\"\"\n    if not isinstance(df, pd.DataFrame):\n        raise DataFrameError(\"Expected a pandas DataFrame\")\n\n    if \"CUADRILLA_OT\" in df.columns and \"CUADRILLA_CORTO\" in df.columns:\n        series = df.query(f\"CUADRILLA_OT == {cuadrilla!r}\")[\"CUADRILLA_CORTO\"]\n        if not series.empty:\n            return str(series.iloc[0])\n\n    try:\n        grupo, tipo = cuadrilla.split(\" Z\")\n    except ValueError:\n        return cuadrilla\n\n    if \"Cuadrilla\" in tipo:\n        return f\"Cuadrilla {grupo}\"\n    if \"Agencia\" in tipo:\n        return f\"Agencia {grupo}\"\n\n    return cuadrilla\n</code></pre>"},{"location":"api/core/#gdrive_utils.core.get_all_data","title":"get_all_data","text":"<pre><code>get_all_data(value, column, df)\n</code></pre> <p>Return every column for the first row where column equals value.</p> <p>All values are returned as strings, except <code>USER_ID</code> which is converted to an <code>int</code>.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>The value to look up.</p> required <code>column</code> <code>str</code> <p>The DataFrame column to search in.</p> required <code>df</code> <code>DataFrame</code> <p>The personnel DataFrame.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>A dictionary with keys for every column in df.</p> <p>Raises:</p> Type Description <code>DataFrameError</code> <p>If df is not a DataFrame, column does not exist, or no matching row is found.</p> Source code in <code>src/gdrive_utils/core.py</code> <pre><code>def get_all_data(value: str, column: str, df: pd.DataFrame) -&gt; dict:\n    \"\"\"Return every column for the first row where *column* equals *value*.\n\n    All values are returned as strings, except ``USER_ID`` which is\n    converted to an ``int``.\n\n    Args:\n        value: The value to look up.\n        column: The DataFrame column to search in.\n        df: The personnel DataFrame.\n\n    Returns:\n        A dictionary with keys for every column in *df*.\n\n    Raises:\n        DataFrameError: If *df* is not a DataFrame, *column* does not exist,\n            or no matching row is found.\n\n    \"\"\"\n    if not isinstance(df, pd.DataFrame):\n        raise DataFrameError(\"Expected a pandas DataFrame\")\n    if column not in df.columns:\n        raise DataFrameError(f\"Column '{column}' not found in DataFrame\")\n\n    mask = df[column] == value\n    matches = df[mask]\n\n    if matches.empty:\n        raise DataFrameError(\n            f\"No match found for '{value}' in column '{column}'\"\n        )\n\n    if len(matches) &gt; 1:\n        logger.warning(\n            \"Multiple matches (%d) for '%s' in '%s'; returning first row\",\n            len(matches),\n            value,\n            column,\n        )\n\n    row = matches.iloc[0]\n    result: dict = {}\n    for col in df.columns:\n        val = row[col]\n        if col == \"USER_ID\":\n            try:\n                result[col] = int(val)\n            except (ValueError, TypeError) as exc:\n                raise DataFrameError(\n                    f\"USER_ID value '{val}' cannot be converted to int\"\n                ) from exc\n        else:\n            result[col] = str(val)\n\n    return result\n</code></pre>"},{"location":"api/core/#gdrive_utils.core.build_filename","title":"build_filename","text":"<pre><code>build_filename(data, df)\n</code></pre> <p>Build a standardised filename from data and personnel df.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>A dictionary with keys <code>cuadrilla</code>, <code>responsable</code> (list), and <code>fecha</code>.</p> required <code>df</code> <code>DataFrame</code> <p>The personnel DataFrame.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The generated filename.</p> <p>Raises:</p> Type Description <code>DataFrameError</code> <p>If required DataFrame columns are missing.</p> <code>KeyError</code> <p>If required keys are missing from data.</p> Source code in <code>src/gdrive_utils/core.py</code> <pre><code>def build_filename(data: dict, df: pd.DataFrame) -&gt; str:\n    \"\"\"Build a standardised filename from *data* and personnel *df*.\n\n    Args:\n        data: A dictionary with keys ``cuadrilla``, ``responsable`` (list),\n            and ``fecha``.\n        df: The personnel DataFrame.\n\n    Returns:\n        The generated filename.\n\n    Raises:\n        DataFrameError: If required DataFrame columns are missing.\n        KeyError: If required keys are missing from *data*.\n\n    \"\"\"\n    cuadrilla = data[\"cuadrilla\"]\n    responsable_name = data[\"responsable\"][0]\n    fecha_raw = data[\"fecha\"]\n\n    num_cuadrilla = get_cuadrilla_order(cuadrilla, df)\n    num_responsable = get_responsable_order(responsable_name, df)\n    iniciales = get_initials(responsable_name, df)\n    cuadrilla_corto = get_short_cuadrilla_name(cuadrilla, df)\n\n    try:\n        fecha = fecha_raw.split(\"T\")[0]\n    except (AttributeError, IndexError):\n        fecha = str(fecha_raw)\n\n    return (\n        f\"OT [{num_cuadrilla}] {cuadrilla_corto} {fecha} \"\n        f\"({num_responsable}) {iniciales}.pdf\"\n    )\n</code></pre>"},{"location":"api/exceptions/","title":"Exceptions","text":""},{"location":"api/exceptions/#gdrive_utils.exceptions","title":"gdrive_utils.exceptions","text":"<p>Custom exception hierarchy for gdrive-utils.</p>"},{"location":"api/exceptions/#gdrive_utils.exceptions-classes","title":"Classes","text":""},{"location":"api/exceptions/#gdrive_utils.exceptions.GDriveUtilsError","title":"GDriveUtilsError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all gdrive-utils errors.</p>"},{"location":"api/exceptions/#gdrive_utils.exceptions.AuthenticationError","title":"AuthenticationError","text":"<p>               Bases: <code>GDriveUtilsError</code></p> <p>Raised when Google authentication fails.</p>"},{"location":"api/exceptions/#gdrive_utils.exceptions.SpreadsheetNotFoundError","title":"SpreadsheetNotFoundError","text":"<p>               Bases: <code>GDriveUtilsError</code></p> <p>Raised when the specified spreadsheet cannot be found or accessed.</p>"},{"location":"api/exceptions/#gdrive_utils.exceptions.WorksheetNotFoundError","title":"WorksheetNotFoundError","text":"<p>               Bases: <code>GDriveUtilsError</code></p> <p>Raised when the specified worksheet cannot be found in the spreadsheet.</p>"},{"location":"api/exceptions/#gdrive_utils.exceptions.DataFrameError","title":"DataFrameError","text":"<p>               Bases: <code>GDriveUtilsError</code></p> <p>Raised when a DataFrame operation fails or data is missing/invalid.</p>"},{"location":"api/exceptions/#gdrive_utils.exceptions.FileOperationError","title":"FileOperationError","text":"<p>               Bases: <code>GDriveUtilsError</code></p> <p>Raised when a file operation (move, rename, etc.) fails.</p>"},{"location":"api/files/","title":"Files","text":""},{"location":"api/files/#gdrive_utils.files","title":"gdrive_utils.files","text":"<p>File-operation utilities (modern API).</p>"},{"location":"api/files/#gdrive_utils.files-classes","title":"Classes","text":""},{"location":"api/files/#gdrive_utils.files-functions","title":"Functions:","text":""},{"location":"api/files/#gdrive_utils.files.move_file","title":"move_file","text":"<pre><code>move_file(\n    current_file_path,\n    new_name,\n    processed_dir=\"ot_procesados\",\n)\n</code></pre> <p>Move current_file_path into a processed_dir sub-directory.</p> <p>The file is placed under its original parent and renamed to new_name.</p> <p>Parameters:</p> Name Type Description Default <code>current_file_path</code> <code>str</code> <p>Absolute or relative path to the source file.</p> required <code>new_name</code> <code>str</code> <p>The desired filename (not full path) at the destination.</p> required <code>processed_dir</code> <code>str</code> <p>Name of the sub-directory created inside the source file's parent directory.</p> <code>'ot_procesados'</code> <p>Returns:</p> Type Description <code>str</code> <p>The full destination path.</p> <p>Raises:</p> Type Description <code>FileOperationError</code> <p>If the source file does not exist, the destination is a directory, or the move fails for any other reason.</p> Source code in <code>src/gdrive_utils/files.py</code> <pre><code>def move_file(\n    current_file_path: str,\n    new_name: str,\n    processed_dir: str = \"ot_procesados\",\n) -&gt; str:\n    \"\"\"Move *current_file_path* into a *processed_dir* sub-directory.\n\n    The file is placed under its original parent and renamed to *new_name*.\n\n    Args:\n        current_file_path: Absolute or relative path to the source file.\n        new_name: The desired filename (not full path) at the destination.\n        processed_dir: Name of the sub-directory created inside the source\n            file's parent directory.\n\n    Returns:\n        The full destination path.\n\n    Raises:\n        FileOperationError: If the source file does not exist, the destination\n            is a directory, or the move fails for any other reason.\n\n    \"\"\"\n    parent = os.path.dirname(current_file_path)\n    destination_folder = os.path.join(parent, processed_dir)\n\n    if not os.path.exists(destination_folder):\n        os.makedirs(destination_folder)\n        logger.info(\"Created destination folder: %s\", destination_folder)\n\n    new_file_path = os.path.join(destination_folder, new_name)\n\n    if os.path.isdir(new_file_path):\n        logger.error(\n            \"Destination '%s' is a directory; cannot overwrite with a file.\",\n            new_file_path,\n        )\n        raise FileOperationError(\n            f\"Destination '{new_file_path}' is a directory\"\n        )\n\n    try:\n        shutil.move(current_file_path, new_file_path)\n        logger.info(\n            \"Moved '%s' -&gt; '%s'\",\n            os.path.basename(current_file_path),\n            new_file_path,\n        )\n        return new_file_path\n    except FileNotFoundError as exc:\n        logger.error(\"Source file not found: %s\", current_file_path)\n        raise FileOperationError(\n            f\"Source file not found: {current_file_path}\"\n        ) from exc\n    except shutil.Error as exc:\n        logger.error(\"Error moving file: %s\", exc)\n        raise FileOperationError(f\"Error moving file: {exc}\") from exc\n    except OSError as exc:\n        logger.error(\"OS error during move: %s\", exc)\n        raise FileOperationError(f\"OS error during move: {exc}\") from exc\n</code></pre>"},{"location":"api/sheets/","title":"Sheets","text":""},{"location":"api/sheets/#gdrive_utils.sheets","title":"gdrive_utils.sheets","text":"<p>Google Sheets reading utilities (modern API).</p>"},{"location":"api/sheets/#gdrive_utils.sheets-classes","title":"Classes","text":""},{"location":"api/sheets/#gdrive_utils.sheets-functions","title":"Functions:","text":""},{"location":"api/sheets/#gdrive_utils.sheets.read_worksheet","title":"read_worksheet","text":"<pre><code>read_worksheet(config=None)\n</code></pre> <p>Open the configured spreadsheet/worksheet and return a DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>GDriveConfig | None</code> <p>A :class:<code>GDriveConfig</code> instance. If <code>None</code>, default configuration is used.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>DataFrame</code> <p>class:<code>pandas.DataFrame</code> with the worksheet contents.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If Google authentication fails.</p> <code>SpreadsheetNotFoundError</code> <p>If the spreadsheet cannot be found.</p> <code>WorksheetNotFoundError</code> <p>If the worksheet cannot be found.</p> <code>DataFrameError</code> <p>If the worksheet is empty or data cannot be read.</p> Source code in <code>src/gdrive_utils/sheets.py</code> <pre><code>def read_worksheet(config: GDriveConfig | None = None) -&gt; pd.DataFrame:\n    \"\"\"Open the configured spreadsheet/worksheet and return a DataFrame.\n\n    Args:\n        config: A :class:`GDriveConfig` instance. If ``None``, default\n            configuration is used.\n\n    Returns:\n        A :class:`pandas.DataFrame` with the worksheet contents.\n\n    Raises:\n        AuthenticationError: If Google authentication fails.\n        SpreadsheetNotFoundError: If the spreadsheet cannot be found.\n        WorksheetNotFoundError: If the worksheet cannot be found.\n        DataFrameError: If the worksheet is empty or data cannot be read.\n\n    \"\"\"\n    if config is None:\n        config = GDriveConfig()\n\n    try:\n        client = authorize(config)\n    except AuthenticationError:\n        raise\n\n    try:\n        sheet = client.open(config.spreadsheet_name)\n        logger.info(\n            \"Successfully opened spreadsheet: '%s'\", config.spreadsheet_name\n        )\n    except gspread.SpreadsheetNotFound as exc:\n        logger.error(\n            \"Spreadsheet '%s' not found. Ensure it is shared with the service account.\",\n            config.spreadsheet_name,\n        )\n        raise SpreadsheetNotFoundError(\n            f\"Spreadsheet '{config.spreadsheet_name}' not found\"\n        ) from exc\n\n    try:\n        worksheet = sheet.worksheet(config.worksheet_name)\n        logger.info(\"Selected worksheet: '%s'\", worksheet.title)\n    except gspread.WorksheetNotFound as exc:\n        logger.error(\n            \"Worksheet '%s' not found in spreadsheet '%s'\",\n            config.worksheet_name,\n            config.spreadsheet_name,\n        )\n        raise WorksheetNotFoundError(\n            f\"Worksheet '{config.worksheet_name}' not found\"\n        ) from exc\n\n    try:\n        data = worksheet.get_all_values()\n    except gspread.GSpreadException as exc:\n        logger.error(\"Error reading worksheet data: %s\", exc)\n        raise DataFrameError(f\"Error reading worksheet data: {exc}\") from exc\n\n    if not data:\n        logger.warning(\"The selected worksheet appears to be empty.\")\n        raise DataFrameError(\"Worksheet is empty\")\n\n    headers = data[0]\n    df = pd.DataFrame(data[1:], columns=headers)\n    logger.info(\"Data successfully imported into DataFrame\")\n\n    # Drop rows with empty NOMBRE values (matches original logic)\n    if \"NOMBRE\" in df.columns:\n        df.loc[df[\"NOMBRE\"] == \"\", \"NOMBRE\"] = pd.NA\n        df = df.dropna(subset=[\"NOMBRE\"])\n\n    return df\n</code></pre>"}]}